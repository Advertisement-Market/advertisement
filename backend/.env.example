# Production datasource (used by the `prod` Spring profile). Copy to `.env` and fill in.
DB_URL=jdbc:postgresql://localhost:5432/adbasket
DB_USERNAME=adbasket
DB_PASSWORD=change-me
# Google OAuth 2.0 Web Client ID (optional - enables Google Sign-in)
GOOGLE_CLIENT_ID=

