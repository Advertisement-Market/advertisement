# Hot-reloadable feature policy — edit this file and POST /actuator/refresh
# to apply changes without a restart. Committed with everything enabled as
# the safe default for local dev and CI; override per-environment as needed.
app:
  roles:
    enabled: [MEMBER, ADVERTISER, OWNER, AGENCY]
  auth-providers:
    enabled: [LOCAL, GOOGLE]